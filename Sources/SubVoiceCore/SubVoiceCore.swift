/// Thư viện thuần của SubVoice: không I/O, không UI, không API hệ thống.
/// Mọi thứ ở đây phải test được mà không cần màn hình hay loa.
public enum SubVoiceCore {
    public static let version = "0.1.0"
}
